

import './bot.css';

export default function BBB({name,children}){
    return(
        <div className={"mohamed"}>
           <button className={"allouche"}> {name}</button>
           {children}
        </div>

    );


}